services:
  - type: web
    name: insurance-broker-dashboard
    env: python
    plan: free
    buildCommand: pip install -r Requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
